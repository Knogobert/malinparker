// ? Add your new NetlifyCMS collection type slugs here and then add it to the Vuex store. Same as collection type `name`
export const CUSTOM_POSTS = ['blog', 'projects']

// ? Set the default color mode 'dark' or 'light'
export const COLOR_MODE_FALLBACK = 'dark'
